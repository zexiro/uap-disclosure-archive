scikit-learn>=1.4
numpy>=1.26
imagehash>=4.3
Pillow>=10

# ASGI server (replaces ThreadingHTTPServer)
uvicorn[standard]>=0.29
starlette>=0.37
aiofiles>=23.2

# AI / Q&A pipeline
anthropic>=0.40       # /api/enrich (Claude with web_search tool)
openai>=1.50          # /api/ask via OpenRouter (OpenAI-compatible API)
fastembed>=0.3        # local ONNX embeddings: text (BAAI/bge-small-en-v1.5) + image CLIP (Qdrant/clip-ViT-B-32-vision)
faiss-cpu>=1.7        # FAISS IndexFlatIP for image similarity search (/api/image/similar)
spacy>=3.7,<4         # NER over OCR text — auto-downloads en_core_web_sm on first use

# Geospatial hotspot analysis
h3>=4.0               # H3 hierarchical hexagonal grid (lat/lng → cell)
esda>=2.7             # Getis-Ord Gi* via PySAL
libpysal>=4.10        # KNN spatial weights for esda

# Knowledge graph + community detection
networkx>=3.2         # unified graph, Louvain (built-in 3.x), centrality metrics
